import 'package:flutter/material.dart';

class ResultFree4AllN extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.black,
        title: Text('XTag Battle Result'),
      ),
      body: SingleChildScrollView(
        child: Container(),
      ),
    );
  }
}
